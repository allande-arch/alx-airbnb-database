# Complex Joins Queries

- **INNER JOIN**: Matches bookings with guests.
- **LEFT JOIN**: Shows all properties, even those without reviews.
- **FULL OUTER JOIN**: Emulated using `UNION` (MySQL lacks native FULL JOIN).